trigger: none

pr: none

variables:
- template: ../config/common/common.yml

stages:

- stage: ValidateResources
  displayName: Validate Resources

  jobs:
  - job: Bicep_Validation
    displayName: Bicep Deployment Validation
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: AzureCLI@2
      displayName: 'Bicep Main Templates Validation'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        workingDirectory: $(System.DefaultWorkingDirectory)/$(subscriptionPathFromRoot)
        inlineScript: |
          $(var-bashPreInjectScript)
          az deployment mg validate --management-group-id $(mgid) --location $(deploymentRegion) --name 'validate-move-sub' --template-file $(System.DefaultWorkingDirectory)/landingzone/move-subscription.bicep --parameters movesub.parameters.json
          $(var-bashPostInjectScript)
    - task: AzureCLI@2
      displayName: 'Get Bicep Main Template Deployment What-If Result'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        workingDirectory: $(System.DefaultWorkingDirectory)/$(subscriptionPathFromRoot)
        inlineScript: |
          $(var-bashPreInjectScript)
          az deployment mg what-if --management-group-id $(mgid) --location $(deploymentRegion) --name 'whatif-move-sub' --template-file $(System.DefaultWorkingDirectory)/landingzone/move-subscription.bicep --parameters movesub.parameters.json
          $(var-bashPostInjectScript)

- stage: DeployResources
  displayName: Deploy Resources

  jobs:
  - job: DeployResourcesInTheEnvironment
    displayName: Deploy Resources in the Environment
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: AzureCLI@2
      displayName:  Move Subscription
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        workingDirectory: $(System.DefaultWorkingDirectory)/$(subscriptionPathFromRoot)
        inlineScript: |
          $(var-bashPreInjectScript)
          az deployment mg create --management-group-id $(mgid) --location $(deploymentRegion) --name 'move-subscripton' --template-file $(System.DefaultWorkingDirectory)/landingzone/move-subscription.bicep --parameters movesub.parameters.json
          $(var-bashPostInjectScript)